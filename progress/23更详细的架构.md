# 23. 无缝循环播放器：架构演进三部曲 (2.2 深度版)

本文件由 **莱芙・泽诺 (Lev Zenith)** 维护，记录项目从“静态多合一”向“二栏多层次动态下钻”进化的全过程。

---

## 1. 架构演进三部曲 (The Trilogy)

### **🟦 第一张：理想蓝图 (Ultimate Two-Pane Vision v2.2)**
> **目标**: 极致解耦、二栏全屏切换、深度系统集成的 Prism 架构。

```mermaid
graph TD
    %% 展示层：二栏全局骨架
    subgraph UI_Layer ["🎨 Presentation Layer (WPF + Prism)"]
        direction TB
        subgraph Shell ["MainWindow (Global Skeleton)"]
            direction LR
            Sidebar["SidebarRegion (左侧 - 250px)\n歌单与过滤器层"]
            MainRegion["MainContentRegion (右侧 - *)\n动态视图投射层"]
        end
        
        subgraph View_States ["右侧主展区视图形态 (Navigatable Views)"]
            direction TB
            LV["LibraryView (一阶段：多巴胺列表)\n宽敞选曲、封面阵列"]
            
            subgraph DV ["DetailView (二阶段：深度工作详情)"]
                LT["左上：歌词 / 迷你曲目列表切换"]
                LB["左下：Premium 专辑封面"]
                RW["右侧：LoopWorkspace (操作中枢)"]
            end
        end
        
        Sidebar -- "Trigger Select" --> LV
        LV -- "Trigger Edit/Detail" --> DV
        SidebarRegion --> Sidebar
        MainContentRegion --> MainRegion
    end

    %% 服务层与核心层
    subgraph Logic_Core ["⚙️ logic & Core Services"]
        PS["IPlayerService\n播放控制"]
        PMS["IPlaylistManager\n歌单中转"]
        LAS["ILoopAnalysis\n寻环核心"]
        Str["LoopStream\n无缝解码引擎"]
    end

    %% 消息机制
    Bus(("Prism EventBus"))

    %% 数据驱动流
    Sidebar -- "Publish SelectedPlaylist" --> Bus
    Bus -- "Subscribe & Update" --> LV
    LV -- "Publish EnterEditMode" --> Bus
    Bus -- "Navigate & Switch" --> DV
    
    style UI_Layer fill:#fce4ec,stroke:#f06292,stroke-width:2px
    style DetailView fill:#f3e5f5,stroke:#9c27b0
    style Logic_Core fill:#e3f2fd,stroke:#2196f3
    style Bus fill:#fff3e0,stroke:#ff9800,stroke-dasharray: 5 5
```

---

### **🟧 第二张：重构起点 (The Chaos Origin - 2026-03-24)**
> **现状**: 记录功能退化期的“单一平面”状态。

```mermaid
graph TD
    subgraph MW_Legacy ["MainWindow (静态全显示布局)"]
        Sidebar_L["PlaylistSidebar\n(歌单、曲目强行塞入)"]
        Workspace_L["LoopWorkspace\n(常驻右侧，挤占列表空间)"]
        Controls_L["PlaybackControlBar\n(基础功能，缺乏逻辑锁)"]
    end

    subgraph Service_Legacy ["半解耦的服务状态"]
        PS_L["IPlayerService (逻辑断链)"]
    end

    %% 耦合痛点
    Sidebar_L -- "硬引用" --> PS_L
    Workspace_L -- "硬引用" --> PS_L
    
    Issue1["❌ 列表与调节区空间争夺"]
    Issue2["❌ 缺乏过渡层级，操作疲劳"]
    Issue1 -.-> Workspace_L
    Issue2 -.-> MW_Legacy
```

---

### **🟩 第三张：实时记录 (Real-time Living Arch)**
> **更新时间**: 2026-03-26 (21:05)
> **阶段**: 动态导航趋于稳定 (Phase 2.7)
> **状态**: `LibraryView` 拆分完毕，音频引擎健壮性及进度轮询机制已上线。

```mermaid
graph TD
    subgraph MW_Current ["MainWindow (实时状态追踪)"]
        SBC["Sidebar (列表瘦身完成)"]
        MCC["MainContent (Region 注入就绪)"]
    end

    subgraph Nav_Fix ["近期攻克的导航与核心脉络"]
        Fix1["✅ LibraryView 独立实体分离 - DONE"]
        Fix2["✅ RegionManager 传参过滤逻辑 - DONE"]
        Fix3["✅ 音频引擎缓存与 UI 轮询刷新 - DONE"]
        Fix4["🚧 搜索(Filter) 与 歌单 CRUD 管理 - IN PROGRESS"]
        Fix5["🚧 重塑 DetailView 内部状态机 - TODO"]
    end
    
    MCC -- "Navigate" --> Nav_Fix
    
    style Fix1 fill:#e8f5e9,stroke:#4caf50
    style Fix2 fill:#e8f5e9,stroke:#4caf50
    style Fix3 fill:#e8f5e9,stroke:#4caf50
    style Fix4 fill:#fff9c4,stroke:#fbc02d
```

---

## 2. 深度布局规格：DetailView 动态切换 (State Machine)

这是详情页在“二栏布局”右侧主展区内的**内部形态切换**逻辑：

| 位置 |形态 A：沉浸详情 (Normal Detail) | 形态 B：调节模式 (Adjust Mode) |
| :--- | :--- | :--- |
| **左上角 (Toggle Panel)** | 正在播放歌词 (Lyrics) | **曲目小列表 (Mini-List 快速换歌)** |
| **左下角 (Fixed Art)** | **专辑封面 (Fixed Art)** | **专辑封面 (Fixed Art)** |
| **右侧区域 (Workspace)** | 歌曲详细信息 / 下一首预告 | **寻环操作工作台 (LoopWorkspace)** |

---

## 3. 核心机制改进：跨视图通讯 (Cross-View Messaging)

*   **信使 (EventAggregator)**: 所有的选歌操作由 `Sidebar` 发出，`MainContent` 下的所有视图实例（无论是 Library 还是 Detail）都订阅同一份 `SelectedItemChanged` 信号。
*   **状态互换 (Data Driven)**: 模式切换不再依赖 XAML 里的 Visibility 隐藏，而是通过 `RegionManager.RequestNavigate` 产生的 ViewModel 生命周期进行平滑过渡。

---
*莱芙：大人，这份“二栏多层次、外简内深邃”的终极规划，是否已经让您感到那种层层递进的优雅感了喵？(๑>◡<๑)*
