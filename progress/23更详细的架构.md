# 23. 无缝循环播放器：架构演进三部曲 (2026-03-24)

本文件由 **莱芙・泽诺 (Lev Zenith)** 维护，记录项目架构从“大单体”向“灵动多巴胺”进化的全过程。

---

## 1. 架构演进三部曲 (The Trilogy)

### **🟦 第一张：理想蓝图 (The Ultimate Dopamine Vision)**
> **目标**: 极致解耦、全功能覆盖的 Prism 架构。

```mermaid
graph TD
    %% 展示层：采用层级嵌套
    subgraph UI_Layer ["🎨 Presentation Layer (WPF + Prism)"]
        direction TB
        subgraph Shell ["MainWindow (Universal Shell)"]
            R1["PrimaryFilterRegion (Sidebar)"]
            R2["MainContentRegion (View Stack)"]
            R3["ControlRegion (Player Bar)"]
        end
        
        subgraph Views ["Functional Views"]
            SBV["SideBarView (Category/Playlist)"]
            LVV["LibraryView (Dopamine Flow)"]
            PCV["PlaybackBarView (Iconic Control)"]
            
            subgraph DV ["DetailView (Deep Workspace)"]
                LT["LeftTop (Lyrics/MiniList Switch)"]
                LB["LeftBottom (AlbumCover)"]
                RW["RightPanel (LoopWorkspace Area)"]
            end
        end
        
        R1 --> SBV
        R2 -- "Nav: Library" --> LVV
        R2 -- "Nav: Detail" --> DV
        R3 --> PCV
    end

    %% 服务层：逻辑中枢
    subgraph Service_Layer ["⚙️ Service & Logic Layer (DI Singleton)"]
        direction LR
        PS["IPlayerService (Audio Logic)"]
        PMS["IPlaylistManager (Collection)"]
        LAS["ILoopAnalysis (DSP Manager)"]
        LS["ILocalization (i18n Engine)"]
    end

    %% 核心层：基础设施
    subgraph Infrastructure ["🛡️ Infrastructure & Asset Core"]
        direction TB
        AL["AudioLooper (State Machine)"]
        Str["LoopStream (Seamless Engine)"]
        DB["Database (SQLite / Dapper)"]
        PyW["PyMusicLooper (AI Wrapper)"]
    end

    %% 通讯机制
    Bus(("Prism EventAggregator"))

    %% 数据与指令流
    UI_Layer -- "Resolved Commands" --> Service_Layer
    Service_Layer -- "Drive" --> AL
    AL -- "Feed" --> Str
    Service_Layer -- "Persistence" --> DB
    LAS -- "Analyze" --> PyW
    
    %% 消息传递
    Service_Layer -. "Broadcast Events" .-> Bus
    Bus -. "Update UI" .-> UI_Layer

    %% 视觉美化方案
    style UI_Layer fill:#fce4ec,stroke:#f06292,stroke-width:2px
    style Service_Layer fill:#e3f2fd,stroke:#2196f3,stroke-width:2px
    style Infrastructure fill:#e8f5e9,stroke:#4caf50,stroke-width:2px
    style Bus fill:#fff3e0,stroke:#ff9800,stroke-dasharray: 5 5
```

---

### **🟧 第二张：重构起点 (The Chaos Origin - 2026-03-24)**
> **现状**: 记录功能退化期的真实痛点。

```mermaid
graph TD
    subgraph MW_Legacy ["MainWindow (The Giant Container)"]
        SB_L["PlaylistSidebar (Merged Tracks)"]
        LW_L["LoopWorkspace (Static View)"]
        PB_L["PlaybackBar (Incomplete Progress)"]
    end

    subgraph Service_Legacy ["Partially Decoupled Services"]
        PS_L["IPlayerService (Broken Links)"]
        PMS_L["IPlaylistManager (WIP)"]
    end

    %% 耦合路径
    SB_L -- "Direct Ref" --> PS_L
    LW_L -- "Direct Ref" --> PS_L
    PB_L -- "Event Conflicts" --> PS_L

    %% 严重故障标注
    Bug1["❌ ProgressBar: Sync Race Issue"]
    Bug2["❌ TrackList: ContextMenu Lost"]
    Bug3["❌ Audio: Prev/Next/Stop Regressed"]
    
    Bug1 -.-> PB_L
    Bug2 -.-> SB_L
    Bug3 -.-> PS_L

    style Bug1 fill:#ffebee,stroke:#c62828
    style Bug2 fill:#ffebee,stroke:#c62828
    style Bug3 fill:#ffebee,stroke:#c62828
```

---

### **🟩 第三张：实时记录 (Real-time Living Arch)**
> **更新记录**: 2026-03-24
> **阶段**: 开始重构地基 (Phase 2.5)
> **说明**: 此图将成为我们的“战功记录仪”。

```mermaid
graph TD
    %% 此刻仍为起点状态，但标注了“即将攻克”的节点
    subgraph MW_Current ["MainWindow (Current Progress)"]
        SBC["PlaylistSidebar (Pending Separation)"]
        LWC["LoopWorkspace (Planned Drawer)"]
        PBC["PlaybackControlBar (Fixing Progress)"]
    end

    subgraph Srv_Current ["Services (Status: 2.5)"]
        PSC["IPlayerService (Fixing Commands)"]
        PMC["IPlaylistManager (Loading Data)"]
    end

    %% 正在执行的改造
    Fix1["🚧 Injecting IsDragging Lock"]
    Fix2["🚧 Designing LibraryView Region"]
    
    Fix1 -.-> PBC
    Fix2 -.-> SBC

    style Fix1 fill:#fff9c4,stroke:#fbc02d
    style Fix2 fill:#fff9c4,stroke:#fbc02d
```

---

## 2. 深度功能规格：DetailView 状态机

| 位置 | 模式 A：听歌 (Normal) | 模式 B：调节 (Adjust) |
| :--- | :--- | :--- |
| **左上角 (Toggle)** | 歌词显示 (Lyrics) | **歌曲列表 (Swapped Mini-List)** |
| **左下角 (Fixed)** | **专辑封面 (Premium Cover)** | **专辑封面 (Premium Cover)** |
| **右侧 (Main)** | **歌曲列表 (Active Playlist)** | **调音工作台 (LoopWorkspace)** |

---

## 3. 实时进展记录 (Arch-Track)

- [x] **2026-03-24 AM**: 确立终极美学架构演进蓝图。
- [ ] **NEXT**: 创建 `RegionNames` 常量，开启 `MainWindow` 的 Prism 领地划分。

---
*莱芙：大人，这套“全状态、高颜值”的架构图，是否已经达到了您的灵魂共鸣？(๑>◡<๑) 如果您满意，莱芙就要按图施工啦！(๑•̀ㅂ•́)و✧*
