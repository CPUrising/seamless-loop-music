<UserControl x:Class="seamless_loop_music.UI.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:seamless_loop_music.UI.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#1E1E2E">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="65"/>  <!-- Column 0: Navigation -->
            <ColumnDefinition Width="280"/> <!-- Column 1: Middle Items -->
            <ColumnDefinition Width="*"/>   <!-- Column 2: Tracks -->
        </Grid.ColumnDefinitions>

        <!-- Column 0: Minimal Navigation -->
        <Border Grid.Column="0" Background="#181825" BorderBrush="#313244" BorderThickness="0,0,1,0">
            <ListBox Background="Transparent" BorderThickness="0" Margin="0,20,0,0"
                     ItemsSource="{Binding NavigationCategories}"
                     SelectedItem="{Binding SelectedCategory}"
                     HorizontalContentAlignment="Center">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Grid Background="Transparent" ToolTip="{Binding Name}">
                                        <Border x:Name="Indicator" Width="3" Height="20" Background="#89B4FA" HorizontalAlignment="Left" Visibility="Hidden" Margin="2,0,0,0"/>
                                        <Border x:Name="ContentBorder" Background="Transparent" CornerRadius="8" Margin="8,4" Padding="10">
                                            <TextBlock Text="{Binding Icon}" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ContentBorder" Property="Background" Value="#313244"/>
                                            <Setter TargetName="Indicator" Property="Visibility" Value="Visible"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ContentBorder" Property="Background" Value="#45475A"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <!-- Column 1: Category Items (Albums/Artists/etc) -->
        <Grid Grid.Column="1" Background="#1E1E2E" Margin="0,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Search -->
                <RowDefinition Height="*"/>    <!-- List -->
            </Grid.RowDefinitions>
            
            <Border Grid.Row="0" Margin="15,20,15,10" Background="#313244" CornerRadius="6" Height="36">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" VerticalAlignment="Center" Margin="10,0,5,0" Foreground="#6C7086" IsHitTestVisible="False" FontSize="12"/>
                    <TextBox Grid.Column="1" 
                             Text="{Binding MiddleFilterText, UpdateSourceTrigger=PropertyChanged}"
                             Background="Transparent" BorderThickness="0" 
                             Foreground="White" VerticalContentAlignment="Center"
                             FontSize="13" CaretBrush="#89B4FA"
                             Margin="2,0,10,0"/>
                </Grid>
            </Border>

            <ListBox Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="5,0,5,10"
                     ItemsSource="{Binding CategoryItemsView}"
                     SelectedItem="{Binding SelectedCategoryItem}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bg" Background="Transparent" CornerRadius="8" Margin="8,2" Padding="8,6">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="40"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <!-- Thumbnail Placeholder -->
                                            <Border Grid.Column="0" Width="36" Height="36" CornerRadius="4" Background="#313244">
                                                <TextBlock Text="{Binding Icon, TargetNullValue=💿}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                                            </Border>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                                                <TextBlock Text="{Binding Name}" Foreground="White" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding Type}" Foreground="#6C7086" FontSize="11" Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bg" Property="Background" Value="#313244"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bg" Property="Background" Value="#45475A"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Grid>

        <!-- Column 2: Content Area (Track List / Detail View) -->
        <Border Grid.Column="2" Background="Transparent">
            <ContentControl prism:RegionManager.RegionName="LibraryContentRegion" />
        </Border>
    </Grid>
</UserControl>
