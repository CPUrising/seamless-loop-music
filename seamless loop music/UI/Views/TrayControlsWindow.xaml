<Window x:Class="seamless_loop_music.UI.Views.TrayControlsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:converters="clr-namespace:seamless_loop_music.UI.Converters"
        xmlns:controls="clr-namespace:seamless_loop_music.UI.Controls"
        Title="TaskbarPreview" Height="255" Width="300"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True" ResizeMode="NoResize"
        Deactivated="Window_Deactivated">
    
    <Window.Resources>
        <ResourceDictionary>
            <converters:PlayPauseIconConverter x:Key="PlayPauseIconConverter" />
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/Colors.xaml" />
                <ResourceDictionary Source="../Themes/Controls.xaml" />
                <ResourceDictionary Source="../Themes/Icons.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="#1E1E2E" CornerRadius="16" BorderThickness="1" BorderBrush="#313244" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="0" Color="Black" Opacity="0.6"/>
        </Border.Effect>
        
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Track Info -->
                <RowDefinition Height="*"/>    <!-- Progress -->
                <RowDefinition Height="Auto"/> <!-- Controls -->
            </Grid.RowDefinitions>

            <!-- Track Info -->
            <controls:TrackInfoControl Grid.Row="0" ArtSize="80" TitleFontSize="16" SubtitleFontSize="13" ShowAlbum="True" Margin="0,0,0,15" CoverImage="{Binding CurrentTrack.ImagePath}"/>

            <!-- Progress -->
            <controls:ProgressControls Grid.Row="1" VerticalAlignment="Center" Margin="0,5"/>

            <!-- Playback Controls -->
            <controls:PlaybackControls Grid.Row="2" PlayButtonWidth="100" PlayButtonHeight="45" Margin="0,15"/>
        </Grid>
    </Border>
</Window>
