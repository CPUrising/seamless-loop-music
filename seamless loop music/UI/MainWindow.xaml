<Window x:Class="seamless_loop_music.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:seamless_loop_music.UI.Views"
        xmlns:prism="http://prismlibrary.com/"
        Title="{Binding [AppTitle], Source={StaticResource Loc}}" Height="800" Width="1200"
        Icon="/Resources/app_icon.png"
        Background="#1E1E2E" Foreground="#CDD6F4" FontFamily="Microsoft YaHei">
    <Window.Resources>
        <!-- Keep existing styles for now, but consolidate later -->
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar Region -->
            <Border Grid.Column="0" Background="#181825" BorderThickness="0,0,1,0" BorderBrush="#313244">
                <ContentControl prism:RegionManager.RegionName="SidebarRegion" />
            </Border>

            <!-- Main Content Region -->
            <Border Grid.Column="1">
                <ContentControl prism:RegionManager.RegionName="MainContentRegion" />
            </Border>
        </Grid>

        <!-- Bottom Bar: Playback Controls (Always visible) -->
        <Border Grid.Row="1" Background="#11111B" Padding="15,10" BorderThickness="0,1,0,0" BorderBrush="#313244">
            <views:PlaybackControlBar />
        </Border>
    </Grid>
</Window>
