# 更改计划

## 需求描述
修复点击歌单后切歌会跳出歌单的问题。

## 实现计划
1. 定位 `OnPlayTrack` 方法（在 `UI\ViewModels\TrackListViewModel.cs:270`）
2. 将第273行的 `Tracks` 改为 `TracksView.Cast<MusicTrack>().ToList()`

## 预期结果
点击歌单后切歌，播放队列保持为当前歌单的过滤结果，而不是全量歌曲。