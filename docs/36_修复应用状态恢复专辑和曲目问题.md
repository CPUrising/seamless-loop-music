# 更改计划

## 需求描述
修复应用退出后重新启动时的状态恢复逻辑：
1. **只恢复**上次播放的音乐和播放队列（后台状态）
2. **不恢复** UI 选中的分类，UI 默认显示"所有歌曲"
3. 左侧栏图标分类顺序调整：从上到下为 **歌单 → 专辑 → 艺术家**

## CPU 大人确认事项
- ✅ 恢复时只恢复播放的曲目和队列，不恢复选中的分类
- ✅ 左侧栏的"歌单"分类下已包含"所有歌曲"、"我的收藏"以及用户创建的歌单（无需修改）

## 需要了解的信息
- 当前项目的相关文件：
  - `Services/AppStateService.cs` — 应用状态持久化服务（保存/恢复逻辑）
  - `UI/ViewModels/LibraryViewModel.cs` — 分类列表创建逻辑和顺序
  - `UI/ViewModels/TrackListViewModel.cs` — 曲目列表视图模型
  - `App.xaml.cs` — 应用启动时恢复状态
  - `Models/CategoryItem.cs` — 分类项模型定义
  - `Models/CategoryNavTarget.cs` — 导航分类目标模型（左侧栏顶层分类）

## 当前项目状态
- **现有实现描述**：
  1. `AppStateService.RestoreStateAsync()` 会恢复 `LastCategoryType`、`LastCategoryId`、`LastCategoryName` 并发布 `CategoryItemSelectedEvent`
  2. 左侧栏顺序目前可能是 Album → Artist → Playlist（需确认 `LibraryViewModel` 中 `NavigationCategories` 的初始化顺序）
  3. 播放曲目和队列恢复逻辑（`LastTrackId` + `SetQueue`）工作正常

- **可复用的逻辑**：
  - 播放状态恢复代码无需改动
  - 只需移除分类恢复部分

## 问题分析
1. **恢复逻辑问题**：`AppStateService.RestoreStateAsync()` 第126-144行恢复了分类上下文，导致 UI 打开时显示上次选中的专辑/艺术家，而不是默认的"所有歌曲"
2. **左侧栏顺序问题**：需要调整 `NavigationCategories` 的初始化顺序

## 实现计划

### 1. 修改 `AppStateService.cs`（移除分类恢复）
- 注释或移除第126-144行的"3. 恢复分类上下文"代码块
- 保留"4. 恢复最后播放的曲目并重建队列"逻辑
- 恢复曲目时，不再依赖 `CurrentCategory`，队列重建使用当前显示的"所有歌曲"列表

### 2. 调整 `LibraryViewModel.cs` 左侧栏顺序
- 找到 `NavigationCategories` 的初始化位置
- 确保顺序为：
  1. Playlist（歌单）
  2. Album（专辑）
  3. Artist（艺术家）

### 3. 验证默认显示"所有歌曲"
- 确认 `TrackListViewModel` 默认状态为 `CategoryItem.Id = -1`（所有歌曲）
- 确认导航到 `TrackListView` 时不带分类参数

## 待确认问题
- 无，CPU 大人已确认需求

## 预期结果
修复后，应用重新启动时：
1. ✅ 播放器状态恢复到上次退出的曲目（如果有的话）
2. ✅ 播放队列正确重建，支持上一首/下一首
3. ✅ UI 默认显示"所有歌曲"歌单（不恢复上次选中的分类）
4. ✅ 左侧栏顺序：歌单 → 专辑 → 艺术家
